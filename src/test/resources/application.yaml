spring:
  main:
    banner-mode: off

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: Loxy1517.

  liquibase:
    enabled: false